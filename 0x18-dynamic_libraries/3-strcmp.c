#include "main.h"
#include <string.h>

/**
 * _strcmp - compare two strings
 * @s1: input parameter
 * @s2: input parameter
 *
 * Return: the comparision result
 */
int _strcmp(char *s1, char *s2)
{
	int result;

	result = strcmp(s1, s2);
	return (result);
}
