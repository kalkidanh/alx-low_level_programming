#include <stdio.h>

/**
 * main - print name of program
 * @argc: number of arguments
 * @argv: name of array
 *
 * Return: 0
 */
int main(int argc, char *argv[])
{
	printf("%s\n", argv[argc * 0]);

	return (0);
}
