#include <stdio.h>
#include <string.h>
/**
 * main - Entry point
 *
 *
 * Return: Always 0
 */
int main(void)
{
	char displayed[52];

	strcpy(displayed, "\"programming is like building a multilingual puzzle");
	puts(displayed);
	return (0);
}
